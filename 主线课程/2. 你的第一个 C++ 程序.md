---
作者: Geoyxy [界系Geoyxy]
tags:
  - 知识类型/工程实践
  - 主线
  - 受审状态/通过
updated: 2026-09-20
---
# 你的第一个 C++ 程序

## 1. 使用 VS 开始一个新项目

> [!TIP] **提示**：学习建议
> 以下步骤是继上一节后的内容。如果你在上一节安装完 VS 2026 后，桌面上没有 VS 2026 的图标（即快捷方式），则请从以下第 1 步开始学习，否则建议从以下第 3 步开始学习。  

1. 如果安装完成后，你发现桌面上没有 VS 2026 的图标，请先找到任务栏中的搜索框（以下第一张图），如果找不到搜索框，请按 `Win` + `X`，此时屏幕上应该出现一个菜单（以下第二张图），点击菜单中的“搜索”。然后在搜索框中输入“Visual Studio”，点击右侧的“打开文件位置”（第一张图）。

![[搜索框.png]]

![[快捷链接菜单.png]]

2. 此时应该会弹出一个文件夹窗口（即文件资源管理器），将其中的“Visual Studio”复制到桌面来。

![[复制快捷方式到桌面.png]]

3. 运行 VS，此时可能会弹出一个登录窗口，你可以根据自己的偏好登录，或者跳过登录。

![[登录窗口.png]]

4. 现在你应该会进入个性化设置界面，开发设置选择“Visual C++”，其他根据自己的偏好选择，全部选择完成后，点击“启动 Visual Studio”。

![[个性化设置.png]]

5. 在此时弹出的开始界面中，点击“创建新项目”。

![[开始界面.png]]

6. 在创建新项目界面中，选择“空项目”，注意标签是“C++”，“控制台”，平台用你实际用的平台，我这里是 Windows。

![[创建新项目.png]]

7. 在配置新项目界面中，设置“项目名称”、“位置”、“解决方案名称”。“项目名称”和“解决方案名称”自选，但是应该能反映这节课的内容，比如“hello_world”、"my_first_code"。你最好自己准备一个专门存放本课程项目、代码的文件夹作为“位置”。三者中都建议不要用空格，而是用下划线“\_”代替空格。准备完成后，点击“创建”。

![[配置新项目.png]]

8. 现在你应该进入了如图的窗口，关闭左侧的“Github Copilot 对话助手”和“新增功能”标签页。

![[关闭对话助手和新增功能.png]]

9. 此时，你的界面应该如下，左侧是“解决方案资源管理器”，若没找到“解决方案资源管理器”，请点击顶部的“搜索”，选择“功能搜索”，在搜索框中输入“解决方案资源管理器”，选择搜索结果列表中的第一项。

![[解决方案资源管理器.png]]

![[功能搜索.png]]

10. 右键点击解决方案资源管理器中的“源文件”，在右键菜单中选择“添加”-“新建项”。或者选中“源文件”，按下 `Ctrl` + `Shift` + `A`。

![[添加源文件.png]]

11. 此时会弹出来如下图所示的窗口，把文件名修改为“main.cpp”，点击“添加”。

![[添加新项.png]]

12. 接下来，你的界面应该如图，你将在中间的文本编辑区（蓝框圈出的区域）完成下一章节 [[#2. 第一段代码]] 的内容。

![[文本编辑区.png]]

---

## 2. 第一段代码

### 2.1 示范代码

```cpp
#include <iostream>
using namespace std;

int main()
{
	cout << "Hello, World!" << endl;
	
	return 0;
}
```

### 2.2 开始编程

> [!WARNING] **注意**：全 / 半角字符
> 你可能注意到中文输入法和英文输入法下打出的某些标点符号是不同的，例如`；`（中文的分号）和`;`（英文的分号），其中前者被称为全角字符，后者被称为半角字符。它们被视为两个完全不同的字符。
> 
> 在代码的语法结构（除了注释和字符串的其它任何部分（这两者将在以后学到））中使用的符号大多数情况下应该为半角字符，在编写代码的过程中**确认你的输入法为英文（半角）模式**。
> 
> （另外，`;` 和 `:` 在 VS 的默认字体下长得很像，**注意不要看错了**）

现在请在 VS 的文本编辑区（见 [[#1. 使用 VS 开始一个新项目]] 中第 12 步）中输入以上示范代码（如图）。

你可能注意到第6和第8行的文本前有一段空白，这被称作**缩进**。缩进不影响代码逻辑的执行，但可以使代码更易于阅读，养成良好的缩进习惯有利于你编写美观、易于维护的代码。

一般通过插入几个空格（一般是4个）或**制表符**来实现缩进。一旦你选好了要使用空格还是制表符，应该在整个项目中保持一致。无论如何，你可以通过按下键盘左上方的TAB键来快速打出一格缩进（取决于你编辑器的设置，按TAB可能自动打出四个空格也可能打出制表符，对于VS，你可以检查文本编辑区右下角的“制表符”选项）

![[hello world.png]]

如果你觉得代码的字体很小，可通过文本编辑区中左下角的百分比数字（图中红框圈出）调整字体大小。

编写完成后，按 `F5`（如果你的键盘上有 `Fn` 键，你可能需要按 `Fn` + `F5`） 或点击文本编辑区上方的“本地 Windows 调试器”，过一会后应该弹出来一个黑色窗口（即控制台），其中写有 `Hello, World!`。

![[运行结果.png]]

如图。如果出现了错误，文本编辑区可能会立刻给你标注错误，错误列表中会显示错误的原因（要查看错误列表，请点击文本编辑区底部的“错误列表”）。

![[错误.png]]

图中的错误是把 `endl` 后的分号 `;` 写成了中文的分号 `；`。如果你遇到了错误，请先检查有没有地方用了中文的字符，或者没有在末尾加分号 `;`，如果无法自行找出错误原因，你可以通过错误代码询问 AI 或寻求帮助。

### 2.3 尝试一些想法

现在你可能发现了，程序的输出结果——`Hello, World`，正是你在代码中写的 `"Hello, World"`。

现在，你可以尝试把 `cout << "Hello, World!" << endl;` 分别改为以下代码：

```cpp
cout << "Hello!";
```

```cpp
cout << "Hello," << endl << "World!";
```

```cpp
cout << "Hello,\n 世界!";
```

```cpp
cout << "我" << endl << "爱" << "\n" << "C++!";
```

### 2.4 编程规则

```cpp
#include <iostream>
using namespace std;

int main()
{
	// 在此处编写你的代码
	
	return 0;
}
```

在以后的编程中，将以上代码作为你编程的**模板**，即只在以上的 `// 在此处编写你的代码` 处编写你的代码。

> [!QUESTION] **问题**：这些代码是什么意思，我真的学会编程了吗？  
> 你目前不需要关注模板中代码的意思，以后你会在主线课程中依次学到，这么做是为了让你先从最基础的知识开始学起。如果你想修改以上模板，你应该明确知道你修改的理由。
> 
> 是的，你已经开始学会编程了。准确地说，你已经完成了编程中最重要的一步——**让计算机按照你的意图做事**。虽然你现在只会让它输出一句话，但这和以后让它计算复杂数据、处理图像、运行游戏，本质上是同一件事：你写下指令，计算机执行。
> 
> 所以，请先别急着否定自己。编程不是“背完所有语法才算会”，而是“能写出可运行的代码，并理解它为什么能运行”。你已经做到了第一点，第二点会随着课程推进慢慢补齐。

现在，你应该了解以下 C++ 的**语法规则**：

1. 代码中用作语法结构的字符大多数情况下应该使用半角字符。
2. 变量定义、变量赋值、函数调用等语句的末尾要加分号 `;`。模板代码中的 `#include`（这是预处理命令）、`int main()`（这是声明）、`{ }`（复合语句）不是符合条件的语句，所以不加。
3. `int main()` 下的 `{...}` 内的末尾应该写 `return 0;` 即使你发现不写也没事。（想了解为什么？试试询问 AI）
4. 代码语法结构中的 `" "`, `[ ]`, `{ }`, `< >`（作为尖括号使用时）, `( )` 字符应成对出现
5. 使用英文字母时是区分大小写的（这被称为大小写敏感）。

> [!NOTE] **概念**：语法 Syntax
> **语法（Syntax）** 是一门编程语言规定的“合法写法”，管的是形式对不对，不管意思对不对。
> 
> 违反语法，程序会直接报错，运行不了；语法正确但意思不对，那就是后面要学的语义和逻辑问题了。

以及**代码风格**：

1. 分号 `;` 后一般是新的一行。
2. `{...}`中的代码前有缩进。
3. `main()` 放在 `main.cpp` 文件中。
4. 代码文件应以换行结尾，例如（由于文章篇幅，我一般不会在课程内的其他代码块中的末尾加空行）：
```cpp
#include <iostream>
using namespace std;

int main()
{
	// 在此处编写你的代码
	
	return 0;
}

```

> [!NOTE] **概念**：风格 Style
> **代码风格（Style）** 是程序员处理代码里不影响逻辑执行的细节的偏好。例如，使用4个空格还是制表符进行缩进，是否使大括号单独占用一行，变量命名习惯等。在你的项目中保持良好的代码风格有利于你或协作的其它人在未来重新阅读与维护这些代码。在初学阶段，此项仅作了解。

### 2.6 补充

以下旨在**为你补充编程概念**，让你更加专业。

1. 你按 `F5` 或 `Fn` + `F5` 后到出现控制台前的过程叫作“**构建并运行**”（但日常口语中我们会把它叫作“**编译**”，而编译实际上只是构建过程的一个步骤，你应当把它们区别开）。
2. 在以上过程（即构建过程）中工作的程序，我们日常把它们统称为“**编译器**”（但严格说它们不是同一个程序，它们分为“**链接器**”、“**调试器**”等。）
3. 构建过程中出现的错误，统称为**构建错误**。构建错误是由“编译器”、“链接器”等报出的，VS 的“错误列表”会把它们混在一起显示。日常我们一般会把“构建错误”叫成“编译错误”或“编译器报错”。
4. 你**试图**查找错误、定位原因并进行修改的过程，叫作**调试（Debug）**。调试的核心是“定位并修正程序行为与预期不符的地方”。**没有编译错误，不等于没有错误**。程序能跑但结果不对、崩溃、卡住，这些都属于需要调试的情况。
5. 编译错误 vs 运行时错误 vs 逻辑错误：
	- **编译错误**：代码不符合语法，编译器直接报错，程序根本生成不了。
	- **运行时错误**：程序能生成、能启动，但运行中崩溃，比如除以零、数组越界。
	- **逻辑错误**：程序能跑、不崩溃，但结果不对，比如算错了、输出错了。
	- 这三种里，**逻辑错误最难查**，因为编译器不会给你任何提示。
6. 你写的 `.cpp` 文件里的内容，叫作**源代码（Source Code）**，简称**源码**。它是给人看的文本，编译器要先把它构建成 `.exe` 文件（即可执行文件）才能运行。

> [!TIP] **提示**：学习心态  
>如果你发现以上概念有很多不懂，甚至感到疑惑，**这很正常，不必焦虑**。
>
>你才刚写出第一个 C++ 程序，而“构建”“编译”“链接”“调试”这些词，本来就要在后面的课程里慢慢展开。我提前放在这里，只是让你**先混个眼熟**，不是要你现在全懂。
>
>编程有个特点：**很多概念不是一次就能懂的，而是在反复使用中逐渐清晰**。第一遍模糊，第二遍有点感觉，第三遍用的时候才真正理解。这很正常。
>
>遇到不懂的地方，你可以：
>
>- **先跳过**，继续往下学，很多疑问后面会自然解开；
>- **记下来**，过几节再回头看，往往会有新理解；
>- **动手试**，改一改、跑一跑，比只看文字更容易懂；
>- **问 AI**，问具体哪里不懂，而不是“我全都不懂怎么办”。
>
>但别因为一个概念不懂就停下来死磕。初学阶段最重要的是先让程序跑起来，先建立起“我能写代码”的信心。很多概念，写久了自然就懂了。
>
> 你现在能让控制台里出现 `Hello, World!`，这就已经是一个真正的开始了。剩下的，慢慢来。
