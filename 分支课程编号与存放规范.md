---
tags:
  - 规范
  - 受审状态/通过
updated: 2026-09-11
---
# 支线课程编号与存放规范

本规范用于管理「分支课程」目录下的所有课程文件及其配套图片资源，确保编号规则统一、文件结构清晰、易于扩展和维护。

---

### 一、编号规则

编号格式为：`主题缩写(双大写) + 系列ID(双小写) + 序号(数字)`

| 组成部分      | 长度       | 示例       | 说明                           |
| :-------- | :------- | :------- | :--------------------------- |
| **主题缩写**  | 2 位大写字母  | `LL`     | 课程所属主题，详见主题表                 |
| **系列 ID** | 2 位小写字母  | `aa`     | 该主题下的子系列标识，从 `aa` 开始递增（无含义）  |
| **序号**    | 1 位及以上数字 | `1`、`12` | 该系列中的课程顺序，从 1 开始递增，**不补前导零** |

---

### 二、主题缩写参考表

|   缩写   | 英文全称                         | 主题      | 包含内容举例                                                   |
| :----: | :--------------------------- | :------ | :------------------------------------------------------- |
| **LL** | Low-level                    | 底层      | 内存布局、指针、引用、动态内存、生命周期、内存对齐、虚拟内存                           |
| **OO** | Object-Oriented              | 面向对象    | 类、继承、多态、封装、抽象类、接口、设计模式基础                                 |
| **CP** | Core C++                     | 语言核心特性  | `auto`、`constexpr`、类型别名、RAII、`final`、`override`、`static` |
| **ST** | Standard Library             | 标准库     | STL 容器（`vector`、`map` 等）、算法、迭代器、`string`、智能指针            |
| **TL** | Template                     | 模板与泛型   | 函数模板、类模板、模板特化、可变参数模板、SFINAE                              |
| **CM** | Compilation Model            | 编译与构建   | 预处理、编译链接、头文件、多文件编程、`extern`、`inline`、命名空间                |
| **SY** | System Programming           | 系统编程    | 系统调用、进程/线程、内存映射、文件 I/O、网络编程基础                            |
| **EN** | Engineering                  | 工程实践    | 编码规范、测试框架、调试技巧、性能分析、代码组织                                 |
| **MT** | Multi-threading              | 多线程与并发  | `std::thread`、互斥锁、条件变量、原子操作、内存序                          |
| **GP** | Graphics / Game              | 图形 / 游戏 | 游戏循环、图形 API 基础、物理引擎基础、UE/Unity 底层原理                      |
| **DA** | Data Structures & Algorithms | 数据结构与算法 | 链表、树、图、排序、查找、复杂度分析                                       |

---

### 三、目录结构规范

```
分支课程/
└── <主题缩写>/
    └── <系列ID> <课程名1>/
		├── 图片/
		│	├── <编号>. <课程名1>/
		│	├── <编号>. <课程名2>/
		│	└── .../
        ├── <编号>. <课程名1>.md
        ├── <编号>. <课程名2>.md
        └── ...
```

**示例**（系列课程）：

```
分支课程/
└── LL/
    └── aa 内存的深入理解/
		├── 图片/
		│	├── LLaa1. 内存的深入理解.md/
		│	└── LLaa2. 内存访问的过程.md/
        ├── LLaa1. 内存的深入理解.md
        └── LLaa2. 内存访问的过程.md
```

---

### 四、编号分配示例

| 课程              | 主题  | 系列  | 编号      | 存放路径                                |
| :-------------- | :-- | :-- | :------ | :---------------------------------- |
| 内存的深入理解         | LL  | aa  | `LLaa1` | `分支课程/LL/aa/LLaa1. 内存的深入理解.md`      |
| 内存对齐与缓存行        | LL  | aa  | `LLaa2` | `分支课程/LL/aa/LLaa2. 内存对齐与缓存行.md`     |
| auto            | CP  | aa  | `CPaa1` | `分支课程/CP/CPaa1. auto.md`            |
| 类型别名 Type Alias | CP  | ab  | `CPab1` | `分支课程/CP/CPab1. 类型别名 Type Alias.md` |
| 工程中的纯抽象类的应用     | OO  | aa  | `OOaa1` | `分支课程/OO/OOaa1. 工程中的纯抽象类的应用.md`     |


### 五、注意事项

| 规则 | 说明 |
| :--- | :--- |
| **系列 ID 从 `aa` 开始** | 同一主题下的不同系列按 `aa`、`ab`、`ac` ……递增 |
| **序号从 `1` 开始** | 同一系列内的课程按 `1`、`2`、`3` ……递增，**不补前导零** |
| **单篇课程也用 `aa` 系列** | 单篇课程视为该主题下的第一个系列的第 1 课（`aa1`） |
| **图片目录与编号对应** | 单篇：`分支图片/<编号>/`；系列：`分支图片/<主题>/<系列ID>/<序号>/` |
| **课程名中的空格** | 保留原课程名中的空格，编号与课程名之间用 `. `（点 + 空格）分隔 |

### 六、编号解析示例

以 `LLaa1. 内存的深入理解.md` 为例：

| 部分   | 值         | 含义            |
| :--- | :-------- | :------------ |
| `LL` | Low-level | 底层主题          |
| `aa` | 系列 a      | 该主题下的第 1 个子系列 |
| `1`  | 第 1 课     | 该系列中的第 1 篇课程  |

以 `CPab2. 类型别名进阶.md` 为例：

| 部分 | 值 | 含义 |
| :--- | :--- | :--- |
| `CP` | Core C++ | 语言核心特性主题 |
| `ab` | 系列 b | 该主题下的第 2 个子系列 |
| `2` | 第 2 课 | 该系列中的第 2 篇课程 |
